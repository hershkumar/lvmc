[project]
name = "lvmc"
version = "0.1.0"
description = "Lattice VMC using normalizing flows"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flax>=0.12.2",
    "jax>=0.9.0.1",
    "jupyter>=1.1.1",
    "numpy>=2.4.2",
    "plotly>=6.5.2",
]
